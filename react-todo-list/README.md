# ToDo App — Module 3 (Vite + React)

**Student:** Mandala Bhavani Shankar  
**Project:** Simple To-Do List application (Vite + React)  
**Assignment file included:** `/mnt/data/React Assignment - Module 3.pdf`

---

## ✅ Summary (what this submission contains)
- A modern, responsive To-Do app built with **Vite** and **React** (functional components).
- Key components: `App`, `Header`, `ToDoList`, `ToDoItem`.
- Implements required behaviors: **Add**, **List** (with `.map()` + unique `key`), **Toggle complete**, **Edit**, **Delete**.
- State stored in `App` and passed via props to children.
- Persistent storage using `localStorage`.
- Keyboard accessible interactions and ARIA attributes for accessibility.
- Styling provided in `src/index.css` — modern, clean UI (no external UI libraries used).
- `.gitignore` included to exclude `node_modules`, `dist`, and secrets.

---

## How to run
1. Clone the repo (or unzip the submission folder).
2. Install dependencies:
```bash
npm install
